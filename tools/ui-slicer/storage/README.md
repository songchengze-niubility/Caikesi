# UI Slicer Storage

这里存放 `tools/ui-slicer/index.html` 的 UI 中间素材。

- `ui/full-reference/`：UI 完整参考图、整页截图、完整概念图。
- `ui/backgrounds/`：UI 背景图，或从完整图中单独整理出的背景参考。
- `ui/components/`：手动框选、去底或遮罩后导出的 UI 组件 PNG。

这些文件仍属于上游参考/中间素材。用户明确审核通过并确定进游戏运行前，不要移动到 `assets/resources/art/`。
