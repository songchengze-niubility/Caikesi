# 项目状态

> 当前进度、方向、待办。改了项目就更新这里。最后更新：2026-06-29。

## 最近进展（给新对话）

> 这是新对话的第一落点。每次开发收尾时刷新成 3~6 行：刚做完什么、下一步、有什么坑。

- 刚完成：新增精简版本地 UI 抠图/切片工具 `tools/ui-slicer/index.html`，入口 `npm run ui:slicer`；本次补充 `tools/ui-slicer/storage/ui/` 三类素材目录（完整参考图/背景图/组件）。
- 工具能力：拖入图片后手动框选组件，支持移动/缩放/命名/微调；每框可做边缘 flood-fill 去底、吸管取底色、手工遮罩画笔，导出 PNG 或复制到剪贴板。
- 边界更新：旧的类 VberAI 工作台、`manual_ui_extractor`、`ai_segment_service` 仍保持删除；`ui-slicer` 只作为 Figma 前的上游素材工具，不自动生成 Cocos 资源、不登记 ArtManifest。
- 仍保留铁律：用户明确审核通过前，任何上游视觉参考图、raw crop 或 ui-slicer 导出都不得复制到 `assets/resources/art/`。
- 下一步候选：用 `ui-slicer` 处理 UI 示意图后进 Figma/`docs/visual/` 归档；若要把成品放进游戏，继续走 `美术资源管线` 的透明预览、审核和 alpha 校验。

## 一句话

竖屏整体、**核心战斗横向展开**的多人小队自动战斗微信小游戏（Cocos 3.8.8，`F:\Cgame`）。

## 已完成

- 战斗循环：3 人小队（坦克近战/输出远程/治疗），单排一字阵；敌人从右进场、无碰撞群殴。
- 战斗框架：统一 `stats` 属性表 + 唯一减法伤害公式；维度有 暴击/暴伤(加成式)/格挡/闪避/攻速/伤害加成/伤害减免/射程。
- 关卡系统：`enemyTypes` 怪物类型表（丧尸/疾行者/重装）+ `levels` 关卡表（每波可混多种怪），打完该关所有波即通关；结算页支持下一关/背包/重打，进度存档记录当前关与最高解锁。
- 表现：色块占位 + 战斗飘字 + 可替换的多层视差滚动背景。
- 工具：游戏内实时调参面板（`ConfigPanel`，临时手感调试）；**Excel 导表管线**（`tools/`，多源驱动 + `npm run config` + 校验 + E2E）；本地 `ui-slicer`（手动框选/轻量去底/遮罩画笔 → PNG/Figma，素材暂存 `tools/ui-slicer/storage/ui/`）。
- **UI 与美术产出管线**（上游协作）：GPT 网页生图/提示词 → Figma 整理 → `ai/memory/视觉风格.md` 风格沉淀 → `docs/visual/` 保存参考、提示词、UI/美术示意图。
- **美术资源管线**（`art/` + `resources/art/`）：逻辑键 + 缺图回退色块；ArtManifest 登记 + check-art 校验 + 序列帧动画；接美术不动玩法代码。
- **装备系统**：背包/仓库/每角色 5 装备栏 + 掉落/转移/穿脱 + 持久化 + 点击详情/差值对比 + 列表滑动 + 拖拽投放；装备属性由 `equip.xlsx` 管理（部位基础、品质倍率、roll、附加词条池），胜利掉落由 `battle.xlsx` 的 `dropGroup` 指向 `drop.xlsx` 权重配置，穿戴经 `EffectiveStats` 接进战斗。

## 当前方向 / 约束

- 战斗+关卡、胜利按配置掉装备、结算页、关卡推进、Excel 导表、美术资源管线、装备存储与装备战斗加成均已落地；下一步做验收/数值调优，或上真实美术/UI。
- 美术阶段：仍占位为主（已有占位 PNG 走代码侧管线验证通）；真实美术/UI 先走 `UI与美术产出管线` 做 Figma 和风格沉淀，确认入游戏后再走 `美术资源管线`。
- 性能前置：新增系统、每帧逻辑、渲染/特效/UI、资源加载、规模扩张任务必须按 `ai/skills/性能约束.md` 自检。

## 待办（未做）

- 装备深化：Cocos 预览手测并打磨拖拽/滑动/胜利掉落手感，装备详情面板视觉打磨，`equip.xlsx`/`drop.xlsx` 数值平衡。
- 关卡深化：Cocos 预览验收结算页/下一关/末关体验，后续做 Boss 波、奖励展示打磨。
- 走位操作、技能/Buff/Debuff。
- 真实美术/UI：先做第一轮 GPT 网页版生图 + Figma 风格板/战斗主界面示意；确认成品后再接角色动画、背景图、打击特效。
- 性能：背景静态渐变每帧重画（小浪费，接贴图后归零，暂不优化）。

## 性能基线

> 改了热路径后按 `ai/skills/开发收尾.md` 第 4 步用微信开发者工具测，明显变差即回归。括号内填测量日期。
> 现处色块占位期，尚未做首次真机测量——下次跑微信开发者工具时补上初始数值。

- DrawCall：待测
- 战斗帧率（真机）：待测
- 内存峰值：待测

## 杂项

- 原始骨架是「放置挂机刷装备」demo，GameManager/DataService/EventCenter 仍在仓库里（暂未用于战斗）。
- 场景 `main`，唯一挂载脚本 `BattleEntry`。
