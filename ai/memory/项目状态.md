# 项目状态

> 当前进度、方向、待办。改了项目就更新这里。最后更新：2026-07-12。

## 最近进展（给新对话）

- **(2026-07-12) 第④步难度导出+节奏验证落地，数值平衡四步全部完成（已提交 0052e75）**：按 `docs/superpowers/specs/2026-07-12-difficulty-derive-design.md`（含 §7 偏差回写）——宝石砍档（普通箱 0/Boss 2/章节 3，毕业期望 56.8→17.1 颗）；`Levels.enemyScale` 机制列（本关怪 hp/atk/hp覆盖 全 ×scale，导表校验同关一致）；**模拟标定难度**（derive 内种子化实跑 BattleManager，二分 kPass/kFail，确定性保持、balance:check 幂等）；`snapshot.buildPanels(state,k)` 参数化 + `progress.ts` P(n) 推进曲线；pacing-sim 改吃 derive 导出的每关 18 维"达标面板"（10 项快速回归；"入场必败"检查按偏差 §7.3 移除——模型入场面板高估真实玩家，卡点存在性由推进模拟实测带保证）；新增 `npm run sim:progress` 推进模拟（20 虚拟玩家全真实链路：实跑战斗/真开箱/真合成/真镶嵌/真升级/真卖装返石，不进 verify）。**实测：总局数中位 40、台阶 L4/L7/L10 额外 6/10/8 局、毕业 Lv12 正中锚点，20/20 通关**；难度旋钮 normal 0.75/gate 0.80/boss 0.70。`npm run verify` 全绿。**剩用户 Cocos 预览人工复核手感**（注意 L3 有 7~11 局的方差性小卡点，可裁量）。
- **(2026-07-11) 子计划 B 生成式数值框架落地（已提交 0052e75）**：按 `docs/superpowers/specs/2026-07-11-balance-derive-design.md`（含 §8 实施偏差回写）——新真源 `balance.xlsx`（Shares/Anchors/Caps/Overrides 四表）→ `tools/balance-model/`（power 战力口径 √(DPS×EHP)+平滑防御估值 / snapshot 毕业快照 / solve 去一法阻尼迭代 / economy 快照=期望 / derive 编排）→ `derived.values.generated.ts`（确定性无时间戳）→ seed 引用重建 xlsx；**templates.ts 形状模板层**是 seed+求解器共同真源（防不动点漂移，已实测 derive→seed→config→check 闭环稳定）。解得 kLevel 0.68/kEquip 1.30/kGem 1.20/kInsc 5.16，份额偏差 ≤2pp；经济账：宝箱 1.76/局、石头 9.6/局、合成 tier1=58 石、expBase=343、返石 5/10/20/40/80。`balance:check` 已挂 verify；13 项 pacing 意外保持全绿（其档位是 sim 内部手写平铺不读装备表，但模型已与真实表脱钩，第④步重校）。**已知设计事实移交第④步**：现掉率下毕业宝石期望 56.8 颗 >> 全队 20 孔（宝石严重过剩，可调 Rewards.gemCount 或接受囤积）。
- **(2026-07-11) 养成材料与产出三机制落地（第③步代码项，已提交 0052e75）**：按 `docs/superpowers/specs/2026-07-11-progression-output-design.md`——①宝箱奖励表化：`ChestService.CHEST_REWARD_PROFILE` 硬编码删除，改读 `chest.xlsx` 新 `Rewards` sheet（`ChestConfig.rewards`，现值原样搬、rng seed 串不变，同 seed 开箱逐位等价）；②经验曲线 100 级三段几何：`expToNext` 分段（1~30 ×1.15 / 31~60 ×1.08 / 61~100 ×1.04，Misc 四个 `charGrowth.expSeg*` 旋钮，无旋钮退化旧公式）；③出售返打造石：`SellResult.returnedGems` 改名 `returnedMaterials`，按品质返石（占位 1/2/4/8/15 ≈ 合成价 15~20%）与宝石退回同链落袋。`npm run verify` 全绿。掉率/定价具体数值待子计划 B 反解；计划 `docs/superpowers/plans/2026-07-11-progression-output.md`。
- **(2026-07-11) 属性系统扩展落地（养成框架子计划 A，已提交 0052e75）**：按 `docs/superpowers/specs/2026-07-11-progression-framework-design.md` 完成——`CombatStats` 13→18 维（技能急速 skillHaste + 普攻/技能/单体/群体伤害四拆，`dmgBonus` 收窄为全伤害）；伤害标签管线（普攻/敌攻/普攻弹道=basic+single，瞬发技能按目标数、弹道按穿透/抛物、场地=aoe，同乘区加算不互乘；DoT 走 `BuffInstance.srcMult` 快照吃全伤害+技能伤害，HoT 不乘）；技能急速（计时 ×(1+急速)/计数需求 ÷(1+急速) 保底 1）；双层叠算 `面板=(白板+固定)×(1+百分比)`（EquipStatKey 扩 10 键，三围/移速%归二级池，等级改乘全池含 def）；穿戴等级=装备等级（仅穿戴时校验，老存档不脱）；角色上限 100 级；宝石 1~6 级等比价值（`levelRatio^(lv-1)`，MaterialId 联合同步 1~6）。全部默认 0/等价，`npm run verify` 全绿、13 项 pacing 门槛不动；新词条/铭文/宝石数值均为占位，待子计划 B（balance.xlsx 生成式框架）按份额反解。实施计划 `docs/superpowers/plans/2026-07-11-stat-system-expansion.md`。
- **(2026-07-12) 正前方十字二连斩骨骼版升级为 `attack-combo2-cross-rig-v04`**：v03 仍错误估计人物主体横坐标，把两个肩点放在躯体同一侧，其中一个继续落在围巾区域，用户再次判退。v04 改为读取 `rigid_core` 肩高逐行 alpha，确认胸廓约位于 `x=202～220`，将左右肩分别固定为 `(202,111)/(216,111)`，并把双手、护手和整条剑路同步右移到身体前方；新增 `shoulder_anchor_diagnostic.png` 以蓝/红点直接证明肩点位于胸廓两侧。12 帧 `320×240 RGBA`、地线 `y=228`、透明边界检查通过；当前待用户目测。
- **(2026-07-12) 旧十字二连斩 v01～v03 整身生图路线判退**：即便收敛尺寸并锁真实脚点，胸腔、肩线、腰胯、腿长和头颈连接仍逐帧变化，后处理不可修；保留为判退证据。
- **(2026-07-11) 前一版十二帧水平二连击 `attack-combo2-horizontal-v01` 留作方向判退对照**：其第一击左→右、第二击右→左，不符合用户“始终向正前方斩并形成十字”的要求，不再作为当前候选。
- **(2026-07-11) 最初长围巾剑客完成真正六张人物的横扫特效候选 `attack-sixframe-horizontal-lr-v01`**：用户明确否决“2 张人物重复成 6 帧”，因此以内置 image generation 一次生成完整 3×2 动作表，六张人物分别为蓄力/启动/中段/命中/过冲/回收，`unique_body_hashes=6`；白灰水墨刀光在中段出现、命中最强、随后消散。去底切分后只做共享缩放、sideview 对齐和完整角色帧上移 3px，成品 6 帧 `320×240 RGBA`、约 12fps、总预览 0.55 秒、命中 index 3、地线 `y=228`。前一版两人物混合方案保留为判退对照。
- **(2026-07-11) 最初长围巾剑客两帧攻击库扩展为 `attack-set-v03` 五招方案**：用户指出 v02 的斜扫/上撩/下劈仍偏纵向、单调，因此新增 D“左后→右前水平横扫”和 E“右前→左后水平回扫”，均由内置 image generation 各自一次生成同源 2×1 动作表；去底切分后只做共享缩放、sideview 对齐和完整角色帧平移，五招统一为 2 帧、建议 6fps、命中 index 1、地线 `y=228`。已有左右横扫专项连播与 D→E→A→B→C 整套连播，当前仅在 staging 待用户目测，运行轮换尚未接线。
- **(2026-07-11) 最初长围巾剑客 death 四拍规则正式沉淀进 skill**：用户判定 `death-v01` 的 8 个过渡姿势仍然太多；`death-v02` 不重新生图，只从同一动作源机械选择 0/3/5/7，形成“站立失衡 → 单膝落地 → 肩侧着地 → 完全倒下”四拍。剑仍全程与持剑手连通，无血腥元素；4 帧均为 `320×240 RGBA`，最小边距 8px，内容底边范围 0px，默认单次 8fps，第 3 帧长期保持。该路线和验收边界已写入 `ai/skills/序列帧角色制作.md`，素材仍只在 staging，未进运行资源。
- **(2026-07-11) 最初长围巾剑客 run-v03 获用户确认“好多了”，四拍跑逻辑已固化进 skill**：`run-v02` 虽减少抖动，但躯干直立、双臂站姿且剑朝右前，缺少奔跑/摆腿感；`run-v03` 以 `idle-v03` 锁身份、用 4×1 技术蓝图整组重生，形成“躯干前倾＋空手反摆＋持剑手髋后＋剑向左后拖曳＋左右接触/通过四拍”。默认 4 帧 8fps 正向循环，头部 X/Y 范围 0.542/1.622px，相邻轮廓变化 0.346～0.366，最小边距 8px。用户确认观感明显改善；完整生产规则已写入 `ai/skills/序列帧角色制作.md`，素材仍只在 staging，未进运行资源。
- **(2026-07-11) 最初长围巾剑客 idle 改为单母版 limited animation**：用户指出原始首张参考图并非既有 `swordsman/idle-v02`，按原图另建 `2026-07-11_original-swordsman-v01`；`idle-v01` 因脚点触格判退，`idle-v02` 虽量化锚点稳定但整身八格重绘产生围巾/衣摆/剑柄结构变化，被用户判“生硬+穿帮”。现按用户确认的新方案产出 `idle-v03`：built-in image generation 只建一张母版，`build_limited_idle.py` 仅驱动马尾和围巾远端作错相闭环，头身/手/剑/腿脚刚性区域逐像素一致；相邻轮廓变化从旧版约 1779–3199px 降至 253–617px，根部/循环独立复核通过，当前待用户目测（默认 10fps，想更沉静可试 8fps）。
- **(2026-07-11) 本地序列帧查看器落地**：新增 `tools/sprite-viewer/index.html` 与 `npm run sprite:viewer`，可选择多张图片/整个目录或直接拖入，支持 1–60 FPS、正向循环/往返循环、暂停逐帧、时间轴、缩放、透明背景明暗切换和自然文件名排序；已在真实浏览器验证 8 帧往返为 `1→…→8→7`，端点不重复停顿，20 FPS 下往返一轮为 0.70 秒。
- **(2026-07-11) 战斗流程 2.0 Plan A 技能槽+被动落地（已点验合入 master 并推送）**：2 槽制（每职业主/被动 ≤2，导表校验）；被动三形态经 `PassiveSystem`+永久 Buff（duration=-1）落地——坚壁（tank 受击 15% 石肤）/战旗光环（healer 全队永久 atk+5%）占位；四钩子（普攻命中/直击受击/直击击杀/放技能）+ 一层防递归。dps 砍 ground_smash 补偿 whirlwind 0.8/lethal 3.5；pacing 重校（L3 减负/L7 覆盖-5%/L10 Boss 39000，50 局诊断口径）六连跑全绿。战斗流程 2.0 全部完成，剩 Boss 第 3 段。
- **(2026-07-11) 战斗流程 2.0 Plan B 行军推进已合入 master**：`moveSpeed` 迁入 `CombatStats` 统一属性表（classes/enemyTypes 顶层速度废弃，减速可 Buff 化，占位 `frost` 冰缓）；`BattlePhase` 的 `gap` 替换为 `marching`——清波后按最慢移速行军 `Wave.distance`（表现假象：背景卷动+原地跑，局部坐标不动），末波清完即胜；`Levels` 表 `waveGap` 列换 `distance`。渲染层背景提速 ×3 + "行军中 %"进度。行军表现已获用户点验通过；spec 见 `2026-07-11-combat-flow2-design.md`。
- **(2026-07-11) 战斗框架第 2 段已合入 master**：Bullet 泛化 `Projectile`（直线/穿透/抛物，命中走效果列表）+ `ZoneEffect` 场地（毒池火海，周期施加）+ 控制三件套（击退钳制/嘲讽光环拉仇恨/沉默禁技不停进度）+ 技能 `delivery` 投递列（空=instant，`line/arc/zone` 编码）。护栏 64 弹道/8 场地；12 项新单测；现有 3 技能等价迁移 pacing 不动。剩第 3 段 Boss 脚本化未开工。
- **(2026-07-11) 战斗框架第 1 段已合入 master**：`CombatUnit` 统一单位模型、`Effects.applyEffect` 唯一状态变更入口、数据驱动 Buff 系统（`buff.xlsx`：中毒/战吼/石肤/眩晕/挑衅/沉默占位）、技能效果列表 + 跨表校验、`buffChanged` 事件。设计/计划见 `docs/superpowers/{specs,plans}/2026-07-11-combat-framework*`。
- **(2026-07-11) 水墨剑客 run 8 帧候选通过双人独立质检**：`docs/visual/staging/characters/2026-07-11_ink-runner-v01/run-v06/` 完成整组生图、绿色键去底、重叠切格、`320×240` RGBA 归一、棋盘格/深色 GIF、头颈诊断与 `alpha_qc.json`；8 帧双腿/长剑完整，upperbody 锚点 X/Y 均 0px，头部核心 X/Y 范围约 0.99/2.59px，脚底下缘保留 16px 腾空差。v01～v05 均按原因留档，没有硬换头或跨版本拼帧；当前只停留 staging 待用户目测，未进运行资源。
- **(2026-07-11) 五职业 idle 候选已按固定 8 帧流程产出**：弓箭手、重剑手、长枪战士、医师仍在各自 `idle-v01` 待审核；刺客 `idle-v01` 因大弓步+直臂像定格攻击被用户判僵，`idle-v02` 因第二把匕首被衣尾遮住而由 Codex 判退，整组重生的 `idle-v03` 采用半步微屈膝、落肩收肘、双匕首分离轮廓，现已获用户观感审核通过并冻结为当前候选。全部素材仅在 `docs/visual/staging/characters/`，未进运行资源。
- **(2026-07-11) Codex 固定 8 帧角色流程完成首轮验证**：以用户黑衣水墨剑客参考图跑通 `idle` 整组生图→键色去底→重叠切格→RGBA 归一→侧视双锚→量化/人工审核；`idle-v01` 因左右漂移及“硬截头部”破坏整体关系判退，整组重生的 `idle-v02` 获用户初步认可。新增 `ai/skills/序列帧角色制作.md` 固化四状态×8帧、结构漂移整组重生、透明验收和 Cocos 入库边界；`sprite-keyframe-tool.py` 新增 `background-mode alpha`，并禁止 `sideview` 后叠加会被马尾/长剑带偏的旧视觉平移。
- **(2026-07-10) TypeScript 与入口架构完成治理**：新增 `tsconfig.game.json` / `tsconfig.tools.json`，TypeScript 固定为 5.8.2，并提供 `npm run typecheck`、`npm test`、`npm run verify`；游戏代码、Node 工具不再混用一套编译环境。
- **`BattleEntry.ts` 从 3081 行降至约 600 行**：现只负责生命周期、读档、战斗会话和跨模块编排；启动页进 `ui/BootView.ts`，战场表现进 `ui/BattleStageView.ts`，结算/上阵/镶嵌/合成/宝箱/账号进入 `ui/panels/`。
- **回归全绿**：两套静态编译、17 组共 163 项测试、13 项节奏门槛、美术完整性和 UI alpha 校验全部通过。Cocos 网页预览已实走启动→战斗→装备→结算→再开装备；其余镶嵌/合成/宝箱/切账号仍需继续人工回归，并用微信开发者工具补 DrawCall/帧率/内存基线。
- **(2026-07-11) 全部改动已分组提交并合入 master**：架构治理、`dps/run` 18→6 帧、序列帧 skill + staging 素材、记忆收尾共四组提交，`feat/gem-inscription` 已合并推送 master；协作分工确定为 **Codex 管美术/UI 素材，Claude 管逻辑/实现/接线/测试**。

## 一句话

竖屏整体、核心战斗横向展开的多人小队自动战斗微信小游戏（Cocos Creator 3.8.8，`F:\Cgame`）；玩家从固定职业角色中上阵最多 2 人并调整前后站位。

## 当前已完成

- 战斗/关卡：统一属性与伤害公式、怪物/波次/胜负、第一章 10 关、4/7/10 台阶卡点、自动技能、多人小队、角色经验升级。
- 装备与奖励：背包/仓库/分角色装备栏、掉落/穿脱/锁定/排序/出售、装备等级、合成、宝箱、离线收益、关卡推进与结算。
- 镶嵌与铭文：宝石孔、铭文位、材料消耗/返还、属性接战斗、背包装备入口与独立面板。
- 存档与账号：本地无密码账号名隔离 `save_<accountId>`，老档迁移到 guest，切账号清缓存并复用完整读档链。
- 表现与资源：水墨背景、启动/开始页、第一版战斗 HUD、角色四动作状态与序列帧；怪物/子弹仍为占位表现。
- 工具与门禁：Excel 多源导表、调参/动作预览、UI 切片、侧视视频挑帧管线，以及 `typecheck` / 163 项测试 / pacing / art / alpha 的统一 `verify` 门禁。

## 当前方向 / 约束

- 玩法逻辑放 `combat/` 和各纯逻辑模块，数值放 `config/`/Excel，Cocos 表现放 `ui/`、`BattleStageView`、`Background`；`BattleEntry` 只做组合根，不再内联新增面板或战场绘制。
- 现处色块/临时素材阶段，先验证玩法和交互；角色主路线已定为黑衣墨影半剪影 + 纯侧视序列帧，PartsRig/DragonBones 与 3D 路线仅作判负留档。
- 新增玩法或工具代码必须纳入对应 tsconfig；交付前至少跑 `npm run verify`。热路径改动按 `ai/skills/性能约束.md` 自检。
- 当前在 `master`。数值平衡工程已整体提交（`0052e75`，72 文件：属性扩展/产出机制/生成式框架/难度导出四阶段一体——文件交叠无法分组，消息内分节说明），**未推送**。工作区剩余未提交改动 = Codex 美术线（AGENTS/CLAUDE/README/设计日志/序列帧 skill 文档、`sprite-keyframe-tool.py`/`build_limited_idle.py`/`sprite-viewer/`、staging 素材）+ 两线混编的 `ai/memory/项目状态.md`/`代码地图.md`（提交美术线时一并带上）。协作分工：Codex 产美术/UI 素材并维护 ArtManifest，Claude 做逻辑/实现/接线/测试。

## 待办（按优先级）

1. 数值平衡（**四步全部完成**，specs `2026-07-11-progression-framework-design.md` / `-progression-output-design.md` / `-balance-derive-design.md` / `2026-07-12-difficulty-derive-design.md`）：①组成 ✅ ②成长 ✅ ③产出 ✅ ④难度导出+节奏验证 ✅（推进模拟实测中度锚点达标）。**只剩用户 Cocos 预览人工复核第一章手感**（L3 有方差性小卡点可裁量）。改数值入口：份额/锚点/难度旋钮改 `balance.xlsx`、形状比例改 `tools/balance-model/templates.ts`，然后 `balance:derive → seed:* → config`；改完跑 `sim:progress` 验节奏。
2. Codex 序列帧（Codex 线，Claude 不参与制作）：十字二连斩骨骼版 v02 已加入固定根点的马尾/围巾/外袍自然摆动，并重排手臂—剑—刀光发力链。当前请目测 `2026-07-12_original-swordsman-v01/attack-combo2-cross-rig-v02/cross_rig_preview_dark.gif`；通过后再决定是否作为正式 attack 方案，运行逻辑仍未接线。
3. 微信开发者工具补首次性能基线（用户指示暂缓）：DrawCall、战斗帧率、内存峰值。
4. 上线资源：`resources/art` 分包/远程 Bundle、图集 trim、ASTC/ETC2 压缩纹理；铺量前加敌人/子弹对象池、存档写入合批和规模上限。
6. 后续玩法：战斗框架第 3 段（Boss 脚本化，boss.xlsx 阶段+轮转，spec 已定稿；招式复用第 1/2 段全部能力）；Buff/弹道/场地/控制地基已落，内容填表待数值平衡；技能升级、第二章；后端阶段以 accountId 接入 `RemoteDataSource`。

## 性能基线

> 尚未完成首次微信真机测量；下次 Cocos/微信开发者工具回归时补齐。

- DrawCall：待测
- 战斗帧率（真机）：待测
- 内存峰值：待测

## 杂项

- 场景 `main`，唯一需要在编辑器挂载的脚本仍是 `BattleEntry`。
- 旧放置挂机骨架 `GameEntry` / `GameManager` / `EventCenter` 仍保留，但不参与当前战斗。
