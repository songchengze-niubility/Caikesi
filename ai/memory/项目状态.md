# 项目状态

> 当前进度、方向、待办。改了项目就更新这里。最后更新：2026-06-27。

## 最近进展（给新对话）

> 这是新对话的第一落点。每次开发收尾时刷新成 3~6 行：刚做完什么、下一步、有什么坑。

- 刚完成：装备系统已并入 master，并**过了首轮预览验证**——占位美术已导入(背景+3 角色显示)、装备 UI 跑通(背包/仓库/穿脱/持久化)。期间修了：占位帧闪烁(各帧改同色)、面板层级(战斗渲染统一套 `_battleRoot` 容器，背包面板打开时 `setSiblingIndex` 置顶覆盖)。
- 仓库卫生已收：`.meta` 全入库(uuid 稳定)、`.superpowers/` 忽略、`.gitattributes` 锁 `.meta` 用 LF。**工作区干净、全部已推送**。
- master 现含：战斗+关卡、Excel 导表(多源)、美术资源管线、装备存储系统。单测 `test:inventory` 12/12、`test:art` 10/10。
- 下一步候选：**装备影响战斗**(effective-stats 层接 BattleManager) / 真实美术(Codex 出图走 `美术资源管线` skill) / 技能 Buff。
- 坑/待办：角色 Sprite 现按职业建(roster 同职业多只会渲染不出，有 follow-up 任务卡)；性能基线仍待首测；远端 `origin/feat/equipment-storage` 已合并可删；改数值走 Excel、加职业同步 `SoldierClass` union。

## 一句话

竖屏整体、**核心战斗横向展开**的多人小队自动战斗微信小游戏（Cocos 3.8.8，`F:\Cgame`）。

## 已完成

- 战斗循环：3 人小队（坦克近战/输出远程/治疗），单排一字阵；敌人从右进场、无碰撞群殴。
- 战斗框架：统一 `stats` 属性表 + 唯一减法伤害公式；维度有 暴击/暴伤(加成式)/格挡/闪避/攻速/伤害加成/伤害减免/射程。
- 关卡系统：`enemyTypes` 怪物类型表（丧尸/疾行者/重装）+ `levels` 关卡表（每波可混多种怪），打完该关所有波即通关。
- 表现：色块占位 + 战斗飘字 + 可替换的多层视差滚动背景。
- 工具：游戏内实时调参面板（`ConfigPanel`，临时手感调试）；**Excel 导表管线**（`tools/`，多源驱动 + `npm run config` + 校验 + E2E）。
- **美术资源管线**（`art/` + `resources/art/`）：逻辑键 + 缺图回退色块；ArtManifest 登记 + check-art 校验 + 序列帧动画；接美术不动玩法代码。
- **装备存储系统**（`inventory/`，纯存储层）：背包/仓库/每角色 5 装备栏 + 掉落/转移/穿脱 + 持久化 + 占位 UI。**不影响战斗数值**（下一版接）。

## 当前方向 / 约束

- 战斗+关卡、Excel 导表、美术资源管线、装备**存储层**均已落地；下一步把装备接进战斗（effective-stats）或上真实美术。
- 美术阶段：仍占位为主（已有占位 PNG 走管线验证通），真实美术由 Codex 出图走 `美术资源管线` skill。
- 性能前置：新增系统、每帧逻辑、渲染/特效/UI、资源加载、规模扩张任务必须按 `ai/skills/性能约束.md` 自检。

## 待办（未做）

- **装备影响战斗**（下一版）：effective-stats 计算层把穿戴装备的加成接进 BattleManager；装备带数值属性 + equip.xlsx 定义 + 真实掉落触发。存储层已就绪。
- 走位操作、技能/Buff/Debuff、Boss 波、关卡奖励。
- 真实美术：角色动画、背景图、打击特效。
- 性能：背景静态渐变每帧重画（小浪费，接贴图后归零，暂不优化）。

## 性能基线

> 改了热路径后按 `ai/skills/开发收尾.md` 第 4 步用微信开发者工具测，明显变差即回归。括号内填测量日期。
> 现处色块占位期，尚未做首次真机测量——下次跑微信开发者工具时补上初始数值。

- DrawCall：待测
- 战斗帧率（真机）：待测
- 内存峰值：待测

## 杂项

- 原始骨架是「放置挂机刷装备」demo，GameManager/DataService/EventCenter 仍在仓库里（暂未用于战斗）。
- 场景 `main`，唯一挂载脚本 `BattleEntry`。
