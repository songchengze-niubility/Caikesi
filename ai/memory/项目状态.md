# 项目状态

> 当前进度、方向、待办。改了项目就更新这里。最后更新：2026-07-07。

## 最近进展（给新对话）

> 这是新对话的第一落点。每次开发收尾时刷新成 3~6 行：刚做完什么、下一步、有什么坑。

- **(2026-07-07) 账号存档系统落地**（分支 `feat/account-save`，基于 `feat/gem-inscription` 切出、待合并）：玩家开始页输入账号名（无密码）按账号隔离存档；`core/data/AccountService.ts` 纯逻辑（校验/当前账号/本机列表/`save_<id>` 键/老档 `idle_save_v1`→`save_guest` 迁移，11 项单测 `test:account`）+ `DataService` 键作用域（`IDataSource` 接口不变、玩法零改动）+ BattleEntry 开始页账号行/账号面板（EditBox+账号列表，确认切换即清缓存重跑读档链）。**accountId 预留为将来后端记录数据的用户标识**（`RemoteDataSource` 同接口接入，见设计日志「存档与账号」）。**尚未人工验证**：Cocos 预览手测切换全流程（老档迁 guest/两账号互不串档/非法名提示）。spec/plan 见 `docs/superpowers/{specs,plans}/2026-07-07-account-save*`。

- **(2026-07-06) 部件规范升 v2：12 件骨架落地**（用户提出手腿各加一关节；落到长袍角色=肘关节×2+袍摆前后片，膝对长袍角色无效已否决，spec `2026-07-06-partsrig-v2-12parts-design.md`）：新十二件拆解图（用户重生成，提示词模板 v2 已进 `视觉风格.md`）→ `assemble_parts12.py` 从零校准（组装对照通过；鞋尖反向 flip、剑竖画转横 80°、旋转余量按长边给修了裁剪 bug）→ 12 骨 DB 工程重出（weapon 挂 armFrontLower，旧 8 骨工程留档 `dragonbones-v1-8bones/`）→ 四动作关键帧重写（run 袍摆前后反相+手臂反腿相、attack 蓄力举剑过头→前下劈过冲三段、`test:partsrig` 11 项全绿）；新增 `npm run rig:dump`（TS 动作单一真源 → actions.json）。**已知可接受项**：躯干件自带完整衣袖，大臂件盖其上，举臂时读作垂落袍袖。**下一步（接力点）**：用户 DB 审 12 骨动画（`temp/partsrig-demo/dragonbones/dps_ske.json`，先磨 attack）+ 过目 `parts12_final/assemble_check.png` 组装效果 → 后续仍是 Cocos 接入决策（倾向原生 dragonBones 组件）。
- **(2026-07-05) 美术管线方向日**：① 3D 渲染序列帧路线（image2 立绘→Tripo→Mixamo→Blender 无头渲染）链路全通但**风格判负**（3D/赛璐璐质感 vs 手绘水墨，着色器补不了），脚本沉淀 `tools/sprite3d/`（Blender 4.5 便携版在 `F:\tools`），判负记录见 spec `2026-07-05-3d-sprite-pipeline-design.md`；② **视频拆帧管线（人机分工版）验证通过**：用户即梦+剪映产白底动作视频丢 `docs/visual/staging/characters/<角色>-video/clips/<动作>.mp4` → `tools/sprite-pipeline/split_action.py` 一条命令出 game_keyframes+预览页（dps idle 已跑通，spec `2026-07-05-video-sprite-pipeline-design.md`）；③ **序列帧红线修订**：帧数 ≤10 不变、fps 上限提至 18、「宁短勿卡」，支持快放模式（少帧@高fps）；④ **角色风格转向定案**：极简水墨 Q 版（首个过审形象 `docs/visual/staging/characters/new_dps.png`）。
- **(2026-07-05) 角色动画最终路线定案：8 件标准骨架 + DragonBones**（美术管线方向日终章）：PartsRig 部件缓动 4→5→**8 件标准部件规范（用户定为设计规范：hairBack/head/torso/armFront/armBack/legFront/legBack/weapon，全角色同件同锚点同父子链，动作参数共享）**。素材=每角色双图（完整立绘+八件拆解图，提示词模板见 `视觉风格.md`）→ `tools/sprite-pipeline/slice_sheet.py` 泛洪切件 → `assemble_parts8.py` 组装校准 → **`export_dragonbones.py` 自动绑骨导出 DragonBones 工程**。DB 编辑器已装（`D:\Program Files\Egret\DragonBonesPro`），工程在 `temp/partsrig-demo/dragonbones/dps_ske.json`（含四动作草稿，run 已重做教科书四姿势循环，`npm run test:partsrig` 10 项含相位/振幅/归位断言）。**动画分工定案：用户在 DB 审/手改 ↔ AI 直接写关键帧 JSON 重导出**（自研 `rig:editor` 退役为微调工具）。**下一步（新对话接力点）**：用户 DB 审动画（先磨 attack 打击感）→ 满意后 Cocos 接入决策（倾向原生 dragonBones 组件，自研 PartsRigPlayer 未写、可能不需要）→ 接 BattleEntry 动作状态机替换临时序列帧。
- 美术管线教训（已进设计日志+跨会话记忆）：美学参数必须**真素材**上调（色块只验证系统数学）；生成部件三坑：浅底吃同色衣料（`slice_sheet` 泛洪抠图已根治）、腿件鞋尖朝向画反（组装 `flip` 镜像）、跑步同手同脚（相位断言拦截）；`sprite-keyframe-tool --video-seconds` 默认只采前 1 秒（编排脚本已显式传 0）。
- **(2026-07-05) 镶嵌（宝石）+ 铭文系统落地**（分支 `feat/gem-inscription`，从 master 切出、待合并）：装备加两组独立槽位（宝石孔+铭文位，按品质浮动）；宝石分 5 类型各 1~4 级、可插拔（数据挂 `EquipItem`、A1 拆细 `MaterialId` `gem_<type>_<level>` **替换掉 `gem_shard`/`rune_dust`**）；铭文靠 `rune_scroll` 随机打入可覆盖；加成走 `EffectiveStats` 同一平铺路径；复用宝箱掉落、合成成本改纯打造石；镶嵌面板走 BattleEntry 方法从背包详情「镶嵌」按钮开。新模块 `inlay/`（InlayConfig/InlayModel/InlayStats 纯逻辑）。走 subagent-driven-development 8 Task + 全分支终审（opus）全过、`READY TO MERGE`；跨模块契约（MaterialId 6 点一致、chest→socket→persist→battle→sell 全链路、序列化幸存、旧材料零残留）逐一核实。**只做机制不重调平衡**：宝石/铭文/掉率占位值，`pacing-sim` 不传镶嵌、13 门槛不受影响。**尚未人工验证**：Cocos 预览手测镶嵌面板全流程。设计/计划见 `docs/superpowers/{specs,plans}/2026-07-05-gem-inscription*`。
- **(2026-07-05) 经验→角色升级系统已并入 master**：击杀经验→全体上阵角色各得全额→升级抬 hp/atk（镜像装备等级系数）；`growth/`新模块 + `EffectiveStats` 可选 `levels` 参数 + `BattleEntry` 击杀累计/胜负提交/离线喂养 + 上阵面板显示 `Lv.N`。8 Task + opus 终审全过已合并。**尚未人工验证**：Cocos 预览手测 Lv 显示/随战斗涨级/离线喂养、老存档兼容；角色等级折进 `pacing-sim` 逐关重调待办。
- **(2026-07-05) 上阵系统 + 自动技能系统已并入 master**：多人小队上阵（选人/排序/存盘，`squad/`）+ 自动技能（`timer`/`attackCount` 触发、aoe/nearest/single 目标，`combat/SkillRuntime.ts`）均已落地并经 `sim:pacing` 平衡校准，细节见「已完成」。**尚未人工验证**：Cocos 预览手测上阵面板/双人战斗手感、技能按钮视觉效果，以及更早遗留的 10 关手感、合成流程、切动作脚点。
- 待办：合成档位材料成本/品质权重仍是占位值待调参；序列帧压缩纹理预设（ASTC/ETC2）待人工在 Cocos 编辑器建立并套用。

## 一句话

竖屏整体、**核心战斗横向展开**的多人小队自动战斗微信小游戏（Cocos 3.8.8，`F:\Cgame`）；玩家从固定职业角色里上阵 2 个、排前后站位。

## 已完成

- 战斗循环：当前为 1 人小队（仅 dps，默认近战）；历史 tank/dps/healer 配置仍保留，敌人从右进场、无碰撞群殴。
- 战斗框架：统一 `stats` 属性表 + 唯一减法伤害公式；维度有 暴击/暴伤(加成式)/格挡/闪避/攻速/伤害加成/伤害减免/射程。
- 关卡系统：`enemyTypes` 怪物类型表（丧尸/疾行者/重装/屠夫领主 Boss）+ `levels` 关卡表（每波可混多种怪），打完该关所有波即通关；结算页支持下一关/背包/重打，进度存档记录当前关与最高解锁。**第一章 10 关**已铺（台阶卡点 4/7/10，`sim:pacing` 13 门槛校准全绿）。
- 表现：干净战斗背景图 + 还原稿主界面 UI 已接入；Figma 加载页与 `01 Begin` 开始页切片已接进 BootFlow；小队角色已有 idle/run/attack/death 动作状态与临时序列帧，怪物/子弹仍是色块占位 + 战斗飘字。
- 工具：游戏内实时调参面板（`ConfigPanel`，临时手感调试）+ 默认折叠隐藏的动作预览面板（`ActionPreviewPanel`，选任意 `ArtManifest` 序列帧键预览游戏内 SpriteFrame/.meta/缩放效果）；**Excel 导表管线**；本地 `ui-slicer`；本地 `sprite-keyframe`（按起止秒截视频片段 + 抠白底 + 站地/身体锚点稳定 + 视觉锁定 + 脚尖硬锁 → `game_keyframes` 游戏导入预览 → 序列帧导入）；`sprite-pipeline/split_action.py`（动作视频目录→均匀拆帧=播放fps→抠图/锚点→game_keyframes+预览页，支持快放模式）；`sprite3d/`（3D 渲染序列帧管线，风格判负存档，换厚涂/赛璐璐风格可复活）。
- **UI 与美术产出管线**（上游协作）：GPT 网页生图/提示词 → Figma 整理 → `ai/memory/视觉风格.md` 风格沉淀 → `docs/visual/` 保存参考、提示词、UI/美术示意图；当前 `Cgameui` 已有 `01 Begin` 开始页稿。
- **美术资源管线**（`art/` + `resources/art/`）：逻辑键 + 缺图回退色块；ArtManifest 登记 + check-art 校验 + 序列帧动画；接美术不动玩法代码。
- **装备系统**：背包/仓库/每角色 5 装备栏 + 掉落/转移/穿脱 + 锁定/排序/出售/批量出售 + 持久化 + 点击详情/差值对比 + 列表滑动 + 拖拽投放；装备属性由 `equip.xlsx` 管理（部位基础、品质倍率、roll、附加词条池），胜利掉落由 `battle.xlsx` 的 `dropGroup` 指向 `drop.xlsx` 权重配置，穿戴经 `EffectiveStats` 接进战斗。
- **装备等级 + 合成**：所有装备携带等级（与品质独立叠乘），玩家用材料选部位+档位合成装备，材料够就必出装备（等级/品质按档位配置随机）；`craft.xlsx` 管理档位材料成本与品质权重（成本已改**纯打造石**）。
- **服务层与宝箱**：seed 随机、统一奖励包、胜利奖励服务、装备服务包装、宝箱库存上限、在线击杀/关底宝箱掉落、离线快速模拟同规则、宝箱选择与开箱奖励展示已落地；离线不直接产装备，开箱再把宝箱转成装备/材料且不给金币经验。
- **自动技能系统**：职业固定技能组（`skill.xlsx`），自动释放（timer/attackCount 两种触发器）、三种目标选择（aoe/nearest/single）、只做伤害类；`combat/SkillRuntime.ts` 纯逻辑 + BattleManager 两个钩子 + `skillCast` 事件；3 个技能按钮为冷却状态展示（不可点）；数值经 `sim:pacing` 校准（技能为节奏点缀，台阶门槛主要由关卡 hp 承担）。
- **多人小队上阵**：`roster` 变玩家选择（`squad/SquadModel.ts` 纯逻辑 + `PlayerData.squad` 持久化 + `SquadView` 上阵面板）；角色池=固定职业（class=character 1:1），`squadCap=2` 每场取舍，默认 `tank+dps`，站位=一字阵前后序；`BattleManager` 加 `roster` 入参、`pacing-sim` 按组合参数化并重调 L7/L10 让默认组合 13 门槛复绿。
- **角色成长**（已并入 master）：击杀经验（`EnemyTypes.exp`）→ 全体上阵角色各得全额 → `CharacterGrowthModel` 连续升级/封顶/存档自愈 → `EffectiveStats` 按等级系数（镜像装备 `levelCoefficient`）放大 hp/atk；`BattleEntry` 胜/负都提交、离线收益同样喂养；上阵面板显示 `Lv.N`/满级标记。只做机制，`pacing-sim` 不传等级、13 门槛不受影响，平衡后置。
- **镶嵌（宝石）+ 铭文**（分支 `feat/gem-inscription`，待合并）：装备两组独立槽位（宝石孔+铭文位按品质浮动）；宝石分 5 类型各 1~4 级、可插拔（挂 `EquipItem.gemSockets`、宝石=新 `MaterialId gem_<type>_<level>` 替换旧碎片/粉尘）；铭文靠 `rune_scroll` 随机打入可覆盖；`inlay/`（InlayConfig/InlayModel/InlayStats 纯逻辑）+ `itemInlayStats` 接 `EffectiveStats` 平铺加成；宝箱产宝石/卷轴、合成改纯打造石、出售退回宝石；镶嵌面板走 BattleEntry 方法从背包详情开。只做机制，`pacing-sim` 不传镶嵌、13 门槛不受影响，平衡后置。
- **账号存档**（分支 `feat/account-save`，待合并）：无密码账号名按账号隔离存档（键 `save_<accountId>`），`AccountService` 纯逻辑 + `DataService` 键作用域（`IDataSource` 不变）+ 开始页账号行/切换面板（切换即清缓存重跑读档链）；老档 `idle_save_v1` 自动迁 `guest`；**accountId = 将来后端用户标识**。

## 当前方向 / 约束

- 战斗+关卡、胜利按服务层/配置掉装备、击杀/关底按配置掉宝箱、结算页、关卡推进、Excel 导表、美术资源管线、装备存储与装备战斗加成、装备整理、离线模拟宝箱、宝箱选择与装备/材料开箱展示、多人小队上阵（默认 `tank+dps`）、自动技能、角色成长（已并入）、镶嵌宝石+铭文（`feat/gem-inscription` 待合并）均已落地；当前主要缺口是**人工 Cocos 预览验证**各系统手感 + 各占位数值折进 `pacing-sim` 重调平衡，或继续做新玩法。
- 美术阶段：主背景、第一版主界面 UI、Figma 加载页与 `01 Begin` 开始页已走 `美术资源管线` 入游戏。**角色动画方向已定案（2026-07-05）**：极简水墨 Q 版 + PartsRig 部件缓动为主路线（细节在部件动画里免费、显存趋近单张立绘），复杂表演（Boss 技能等）走视频拆帧管线兜底；怪物/子弹后续按新风格统一。入库流程不变：审核 → assets/resources/art → ArtManifest/校验。
- 性能前置：新增系统、每帧逻辑、渲染/特效/UI、资源加载、规模扩张任务必须按 `ai/skills/性能约束.md` 自检。

## 待办（未做）

- 装备深化：Cocos 预览手测单人 dps 的穿装/锁定/排序/出售/批售/胜利掉落手感，装备详情面板视觉打磨，`equip.xlsx`/`drop.xlsx` 数值平衡。
- 合成/等级深化：`craft.xlsx` 材料成本与品质权重数值平衡；后续评估是否需要装备等级对战斗数值影响的进一步打磨（当前只做了生成时属性缩放）。
- 宝箱深化：Cocos 预览手测宝箱选择/装备材料开箱展示/空间不足不消耗/库存满不新增；后续把开箱倍率配置化，并做材料消耗（打造装备、宝石、铭文）。
- 关卡深化：Cocos 预览手测 10 关手感与台阶卡点体验（ConfigPanel 微调 → 回填 Excel → `sim:pacing` 回归）；后续做第二章、Boss 视觉/机制、奖励展示打磨。
- 走位操作；技能深化：Buff/Debuff 容器、怪物/Boss 技能、技能升级/解锁、手动释放模式评估。
- 上阵深化：Cocos 预览手测上阵面板（选人/排序/存盘生效/老存档回落/下阵保底）与双人战斗手感；后续加职业时 `squadCap` 调大 + 替代组合平衡评估；面板视觉打磨（现为色块占位）。
- 角色成长深化：Cocos 预览手测 Lv 显示/随战斗涨级/离线喂养/老存档兼容；把"每关预期角色等级"折进 `pacing-sim` 的 LOADOUT 逐关重调平衡（当前角色等级会让 10 关随练度变简单，且 sim 尚未覆盖）；`expBase`/`expGrowthPerLevel`/`statGrowthPerLevel`/`maxLevel` 占位数值待调参；可选非阻塞 polish：`CharGrowthConfig.ts`/`BattleEntry.ts` 里多处 `?? 30`/`?? 50`/`?? 1.15` 配置兜底值收敛成共享常量。
- 镶嵌/铭文深化（`feat/gem-inscription` 待合并）：Cocos 预览手测镶嵌面板全流程（背包开面板/镶入取出/打铭文覆盖/卖镶宝石装备退回/已穿戴拦截）；宝石/铭文/宝箱掉率占位值待调参并折进 `pacing-sim`；后续可加宝石合成升级、多种卷轴、套装等（本次明确不做）。可选 polish：`held` 数组用 `GemType`、持有宝石显示上限 6 改滚动。
- 账号存档深化（`feat/account-save` 待合并）：Cocos 预览手测切换全流程（老档迁 guest 无感继承/两账号背包关卡互不串档/非法名红字提示/列表点选）；后端接入时写 `RemoteDataSource` 实现同一 `IDataSource`（身份= `Accounts.currentAccount()`）；账号删除/改名 UI、微信 openid 登录留后端阶段一并议。
- 真实美术/UI：**PartsRig 部件缓动系统落地**（spec→色块 demo→拆 new_dps 四件→接 BattleEntry 替换临时序列帧）；怪物动画、子弹、打击特效按新极简水墨风格跟进；小队其他角色形象按 new_dps 风格重新生成。
- 性能：背景静态渐变每帧重画（小浪费，接贴图后归零，暂不优化）。
- 性能·资源侧（上线阻塞，接真实美术时一并做）：`resources/art` 34.2MB 超微信总包上限 → 远程 Bundle/分包决策 + 序列帧合图集（trim 带 offset，可一并替掉 full-frame 权宜方案）+ ASTC/ETC2 压缩纹理（人工步骤：Cocos 项目设置建预设并套用到 `resources/art/` 贴图，操作见 `美术资源管线.md`「序列帧规格红线」）+ attack 36 帧减半评估。新导素材经 `sprite-keyframe`「游戏帧高上限」自动合规；现有临时序列帧（361×246 超规格）待替换真实美术时按红线重导，不单独返工。
- 性能·铺量前：敌人/子弹对象池 + 最大存活数上限；存档写入 debounce 合批（`PlayerDataStore` 统一入口，微信端换异步 `wx.setStorage`）；引擎模块裁剪（3D/物理）；菜单页 `game.frameRate` 降频；血条美术化后从 Graphics 迁出。

## 性能基线

> 改了热路径后按 `ai/skills/开发收尾.md` 第 4 步用微信开发者工具测，明显变差即回归。括号内填测量日期。
> 现处色块占位期，尚未做首次真机测量——下次跑微信开发者工具时补上初始数值。

- DrawCall：待测
- 战斗帧率（真机）：待测
- 内存峰值：待测

## 杂项

- 原始骨架是「放置挂机刷装备」demo，GameManager/DataService/EventCenter 仍在仓库里（暂未用于战斗）。
- 场景 `main`，唯一挂载脚本 `BattleEntry`。
