# 项目状态

> 当前进度、方向、待办。改了项目就更新这里。最后更新：2026-07-01。

## 最近进展（给新对话）

> 这是新对话的第一落点。每次开发收尾时刷新成 3~6 行：刚做完什么、下一步、有什么坑。

- 已完成装备整理阶段并修正背包/仓库格子点击命中：点击“皮帽”不会再误选上一格“布衣”，Cocos 预览已由用户确认可用。
- 装备支持锁定、排序、出售、批量出售白/绿；锁定与已穿装备不会被出售，出售收益通过 `BattleEntry` 写回 `PlayerData.gold`。
- 验收已跑通：`npm run config`、`test:inventory/services/combat/effective/drop/progression/art`、`check:art`、`check:ui-alpha` 全绿；点击命中修复额外跑过 `test:inventory`。
- 下一步进入阶段 3：宝箱开箱奖励展示正式化。
- 工作区仍有本轮前已有的角色序列帧/`sprite-keyframe` 工具改动未纳入装备/点击修复提交；不要误当作本次玩法改动。

## 一句话

竖屏整体、**核心战斗横向展开**的多人小队自动战斗微信小游戏（Cocos 3.8.8，`F:\Cgame`）。

## 已完成

- 战斗循环：当前为 1 人小队（仅 dps，默认近战）；历史 tank/dps/healer 配置仍保留，敌人从右进场、无碰撞群殴。
- 战斗框架：统一 `stats` 属性表 + 唯一减法伤害公式；维度有 暴击/暴伤(加成式)/格挡/闪避/攻速/伤害加成/伤害减免/射程。
- 关卡系统：`enemyTypes` 怪物类型表（丧尸/疾行者/重装）+ `levels` 关卡表（每波可混多种怪），打完该关所有波即通关；结算页支持下一关/背包/重打，进度存档记录当前关与最高解锁。
- 表现：干净战斗背景图 + 还原稿主界面 UI 已接入；Figma 加载页与 `01 Begin` 开始页切片已接进 BootFlow；小队角色已有 idle/run/attack/death 动作状态与临时序列帧，怪物/子弹仍是色块占位 + 战斗飘字。
- 工具：游戏内实时调参面板（`ConfigPanel`，临时手感调试）+ 默认折叠隐藏的动作预览面板（`ActionPreviewPanel`，选任意 `ArtManifest` 序列帧键预览游戏内 SpriteFrame/.meta/缩放效果）；**Excel 导表管线**；本地 `ui-slicer`；本地 `sprite-keyframe`（按起止秒截视频片段 + 抠白底 + 站地/身体锚点稳定 + 视觉锁定 + 脚尖硬锁 → `game_keyframes` 游戏导入预览 → 序列帧导入）。
- **UI 与美术产出管线**（上游协作）：GPT 网页生图/提示词 → Figma 整理 → `ai/memory/视觉风格.md` 风格沉淀 → `docs/visual/` 保存参考、提示词、UI/美术示意图；当前 `Cgameui` 已有 `01 Begin` 开始页稿。
- **美术资源管线**（`art/` + `resources/art/`）：逻辑键 + 缺图回退色块；ArtManifest 登记 + check-art 校验 + 序列帧动画；接美术不动玩法代码。
- **装备系统**：背包/仓库/每角色 5 装备栏 + 掉落/转移/穿脱 + 锁定/排序/出售/批量出售 + 持久化 + 点击详情/差值对比 + 列表滑动 + 拖拽投放；装备属性由 `equip.xlsx` 管理（部位基础、品质倍率、roll、附加词条池），胜利掉落由 `battle.xlsx` 的 `dropGroup` 指向 `drop.xlsx` 权重配置，穿戴经 `EffectiveStats` 接进战斗。
- **服务层与宝箱**：seed 随机、统一奖励包、胜利奖励服务、装备服务包装、宝箱库存、在线击杀/关底宝箱掉落、离线快速模拟同规则、宝箱开箱最小面板已落地；离线不直接产装备，开箱再把宝箱转成金币/经验/装备。

## 当前方向 / 约束

- 战斗+关卡、胜利按服务层/配置掉装备、击杀/关底按配置掉宝箱、结算页、关卡推进、Excel 导表、美术资源管线、装备存储与装备战斗加成、装备整理、离线模拟宝箱、开箱最小闭环均已落地；当前先按“单人近战 dps”验收/调手感，或继续做宝箱奖励展示打磨。
- 美术阶段：主背景、第一版主界面 UI、Figma 加载页与 `01 Begin` 开始页已走 `美术资源管线` 入游戏；角色动作逻辑已接好但资源仍是临时复用，后续真实角色/怪物/子弹/特效继续按“Figma/审核 → assets/resources/art → ArtManifest/校验”推进。
- 性能前置：新增系统、每帧逻辑、渲染/特效/UI、资源加载、规模扩张任务必须按 `ai/skills/性能约束.md` 自检。

## 待办（未做）

- 装备深化：Cocos 预览手测单人 dps 的穿装/锁定/排序/出售/批售/胜利掉落手感，装备详情面板视觉打磨，`equip.xlsx`/`drop.xlsx` 数值平衡。
- 宝箱深化：把开箱奖励展示做成正式弹窗/列表，开箱倍率配置化，并补材料产出。
- 关卡深化：Cocos 预览验收结算页/下一关/末关体验，后续做 Boss 波、奖励展示打磨。
- 走位操作、技能/Buff/Debuff。
- 真实美术/UI：替换 `assets/resources/art/char/<职业>/<idle|run|attack|death>/` 的临时序列帧，开 Cocos 导入生成 `.meta`；后续再接怪物动画、子弹、打击特效。
- 性能：背景静态渐变每帧重画（小浪费，接贴图后归零，暂不优化）。

## 性能基线

> 改了热路径后按 `ai/skills/开发收尾.md` 第 4 步用微信开发者工具测，明显变差即回归。括号内填测量日期。
> 现处色块占位期，尚未做首次真机测量——下次跑微信开发者工具时补上初始数值。

- DrawCall：待测
- 战斗帧率（真机）：待测
- 内存峰值：待测

## 杂项

- 原始骨架是「放置挂机刷装备」demo，GameManager/DataService/EventCenter 仍在仓库里（暂未用于战斗）。
- 场景 `main`，唯一挂载脚本 `BattleEntry`。
