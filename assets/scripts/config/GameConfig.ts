// 数值配置表（GameConfig）
// ★★★ 这是给策划（你）改的文件 ★★★
// 调整游戏手感、节奏、平衡，绝大多数时候只改这里的数字，不用碰别的代码。
// 改完保存，编辑器会自动重新编译，运行就生效。

export const GameConfig = {
    // 在线挂机：每秒产出多少金币
    goldPerSecond: 10,

    // 角色初始战力
    basePower: 100,

    // 离线收益效率（0 ~ 1）：1 表示离线和在线赚得一样快，0.5 表示离线是在线的一半
    offlineEfficiency: 0.5,

    // 离线收益最多累计多少小时（防止离线太久数值爆炸）
    offlineMaxHours: 8,
};
